import { render } from '@testing-library/react'
import AppContainer from './App'

test('renders learn react link', () => {
   render(<AppContainer />)
})
test('render', () => {
   expect(0).toBe(0)
})
